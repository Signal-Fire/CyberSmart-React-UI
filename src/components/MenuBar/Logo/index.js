import React from 'react';
import { APP_NAME } from '../../../config';

const Logo = () => {
    return (
        <div className= "item">
            <p>{APP_NAME}</p>
        </div>
    );
}

export default Logo;